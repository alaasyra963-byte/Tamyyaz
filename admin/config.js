// Which GitHub repo this admin panel publishes releases to.
// Create a PUBLIC repo (Pages + Releases are free only on public repos),
// enable GitHub Pages on it, then fill these in. See ../README.md.
export const githubConfig = {
  owner: "alaasyra963-byte",
  repo: "Tamyyaz",
};
